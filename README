Integrantes:
    - Felipe Guicharrousse
    - Rodrigo Ramirez

Ejecucion:
    Para la correcta ejecucion del programa se le pedira que ejecute los siguientes comandos
    Dentro de la carpeta raiz del proyecto, ejecutar
        docker-compose build
        docker-compose up
    
    Luego para el ejecucion de los clientes, ejecutar dentro de la carpeta raiz del proyecto
        cd cliente
        go run main.go